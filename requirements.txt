# torch is intentionally NOT pinned here.
# Colab ships a CUDA-matched build; reinstalling it is a slow way to break the
# runtime. Install torch separately if you are running locally:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# --- speech-to-text + embeddings ---
transformers>=4.44,<5
sentence-transformers>=3.0
accelerate>=0.33

# --- audio io ---
soundfile>=0.12
numpy>=1.24,<3

# --- Arabic TTS (MMS/VITS needs romanized input) ---
uroman>=1.3

# --- LLM client (OpenAI wire format -> local Ollama) ---
openai>=1.40

# --- app + types ---
gradio>=4.44
pydantic>=2.7

# --- dev ---
pytest>=8.0
